<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yyty.hao.studyproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light.DarkActionBar">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PatternActivity" />
        <activity android:name=".activity.UIWidgetActivity" />
        <activity
            android:name=".activity.SweepGradientActivity"
            android:label="@string/title_activity_sweep_gradient"
           />
        <activity
            android:name=".activity.JniActivity"
            android:label="@string/title_activity_jni"
           />
        <activity
            android:name=".activity.FirstJniActivity"
            android:label="@string/title_activity_first_jni"
          />
        <activity android:name=".activity.SpringBackActivity" />
        <activity android:name=".activity.FilletImageActivity" />
        <activity android:name=".activity.MatrixActivity" />
        <activity android:name=".activity.CommonKnowledgeActivity" />
        <activity android:name=".activity.AnimationActivity"></activity>
    </application>

</manifest>